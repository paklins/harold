import { FlexBlockDirective } from './flex-block.directive';

describe('FlexBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexBlockDirective();
    expect(directive).toBeTruthy();
  });
});
